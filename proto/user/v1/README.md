# user.v1 proto exercise

ここに `user.proto` を作ります。

最初の課題:

1. `UserService` を定義する
2. `GetUser` RPC を定義する
3. request / response / user message を定義する
4. Go 側の生成コードを作る
5. NestJS 側から同じ proto を読み込む

`.proto` は gRPC の「契約」です。まずここを自分で書くところから始めます。
